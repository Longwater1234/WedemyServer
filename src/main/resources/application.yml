#All entries in ${CURLY_BRACES} should be set as ENV variables, for security reasons.
frontend:
  root:
    url: http://localhost:8080 #replace with valid URL
logging:
  level:
    org:
      hibernate:
        SQL: debug
server:
  error:
    include-stacktrace: never
  port: ${PORT}
  servlet:
    session:
      cookie:
        http-only: true
        max-age: 10800s #3hours
        name: WD_SESSION
spring:
  datasource:
    password: ${MYSQL_PASSWORD}
    url: ${MYSQL_HOST}
    username: ${MYSQL_USERNAME}
  jpa:
    hibernate:
      ddl-auto: update #change to `none` in production
    properties:
      hibernate:
        order_inserts: true
        order_updates: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        jdbc:
          batch-size: 20
  cache:
    type: redis
    redis:
      time-to-live: PT30M #30minutes
  redis:
    host: ${REDIS_TLS_URL}
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
  session:
    store-type: redis
    redis:
      cleanup-cron: 0 1 * * * *
